## Summary

<!-- what changed and why, in 1–3 lines -->

## Verification

- [ ] `pnpm lint` + `pnpm build` + `pnpm test` green (CI-enforced anyway)
- [ ] Base is `develop` (feature/fix) — or this is a `release/*` / `claude/hotfix-*` PR into `main`
- [ ] Docs updated if behaviour, flow, or schema changed

<!-- Opt out of auto-merge: open as a draft, or add the `do-not-merge` label. -->
